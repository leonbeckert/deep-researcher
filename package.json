{
  "name": "deep-researcher-public",
  "private": true,
  "description": "Landing page for deep-research.leon.fm (Cloudflare Worker).",
  "scripts": {
    "build:css": "bash build-css.sh"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  }
}
